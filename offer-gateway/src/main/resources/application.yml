spring:
  application:
    name: offer-gateway
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        username: nacos
        password: nacos
      config:
        server-addr: localhost:8848
        username: nacos
        password: nacos
        file-extension: yaml
  config:
    import:
      - optional:nacos:offer-gateway.yaml
